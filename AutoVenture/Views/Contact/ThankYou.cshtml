@{
    ViewData["Title"] = "Thank You";
}

<section class="max-w-2xl mx-auto py-24 px-5 text-center" data-reveal="hero">
    <div class="brutal-card bg-acid p-10 -rotate-1">
        <p class="font-display text-6xl mb-2">🏁</p>
        <h1 class="font-display text-3xl md:text-4xl uppercase leading-none">Получихме съобщението</h1>
        <p class="mt-4 font-bold">Ще се свържем с теб възможно най-скоро.</p>
        <a href="@Url.Action("Index", "Home")" class="brutal-btn inline-block mt-8 bg-ink text-paper px-8 py-3">← Към началото</a>
    </div>
</section>
